# Load data
STEM_data <- read_dta("../exercise 3/STEM_data_for_adv_ERM.dta") |>
# Drop missing values for the variables we care about
filter(
!is.na(fem),
!is.na(binmothered),
!is.na(bin_prop_female_faculty_stem),
!is.na(stemdeg),
!is.na(income_5yrs)
)
